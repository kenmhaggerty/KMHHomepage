/**
 * Set on <html> while any modal is open, so the page behind it cannot scroll.
 * Shared by the image viewer and the changelog, which use the same shell.
 */
export const MODAL_OPEN_CLASS = 'is-modal-open';
